node_modules
dist
.env
supabase/.temp
test-results
playwright-report
blob-report

# Archivos de bloqueo/backup temporal de Office/AutoCAD (se crean solos mientras el archivo está abierto)
~$*
*.dwl
*.dwl2
*.bak

# Archivos operativos del usuario (ej. la tabla RCL<->MZ armada a mano para
# el import de identidad_legacy) -- no son parte del código, no van al repo.
Docs/Documentos de base de datos/*.xlsx
